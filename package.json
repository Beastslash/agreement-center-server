{
  "type": "module",
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.6",
    "@types/node": "^22.1.0"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  },
  "scripts": {
    "build": "tsc -b --clean && tsc -b"
  }
}
